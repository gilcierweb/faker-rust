# faker_rust::games::WorldOfWarcraft

```rust
// Generate a random character from World of Warcraft
WorldOfWarcraft::hero() //=> "Uther the Lightbringer"

// Generate a random class name from World of Warcraft
WorldOfWarcraft::class_name() //=> "Druid"

// Generate a random class name from World of Warcraft
WorldOfWarcraft::race() //=> "Night elf"

// Generate a random quote from World of Warcraft
WorldOfWarcraft::quote() //=> "These are dark times indeed."
```
